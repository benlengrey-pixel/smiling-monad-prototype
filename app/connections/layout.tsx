import type {
  ReactNode,
} from "react";

type ConnectionsLayoutProps = {
  children: ReactNode;
};

export default function ConnectionsLayout({
  children,
}: ConnectionsLayoutProps) {
  return children;
}